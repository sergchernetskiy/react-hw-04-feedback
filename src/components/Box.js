import styled from '@emotion/styled';
import { color, space, layout, border, shadow } from 'styled-system';

export const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${border}
  ${shadow}
`;
